# Infans Tools public site

This repository contains the browser-ready public deployment artifacts for `kouu-table` and the authenticated JLPT exam shell.

Authoritative source remains in private source repositories. The JLPT artifact contains only the login and exam UI; questions, answers, account credentials, and audio remain in private cloud storage behind authentication and row-level access rules.
